# Core ML framework
torch>=2.5.0
accelerate>=0.25.0

# Hugging Face
transformers>=5.0.0
datasets>=2.14.0

# Audio
soundfile>=0.12.1
torchaudio>=2.1.0

# Optional but recommended
flash-attn>=2.5.0  # FlashAttention-2 for faster attention (CUDA only)
deepspeed>=0.14.0  # Multi-GPU distributed training
